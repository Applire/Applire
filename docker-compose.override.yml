# Copyright (C) 2024-2026 Tobias Rosenbaum
#
# This file is part of Applire.
#
# Applire is free software: you can redistribute it and/or modify
# it under the terms of the GNU Affero General Public License as published
# by the Free Software Foundation, either version 3 of the License, or
# (at your option) any later version.
#
# Applire is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE. See the
# GNU Affero General Public License for more details.
#
# You should have received a copy of the GNU Affero General Public License
# along with Applire. If not, see <https://www.gnu.org/licenses/>.

# Development overrides — auto-applied by Docker Compose when present alongside docker-compose.yml.
# Builds images from source, mounts code for hot-reload, and adds nginx at port 80.
# Developers: cp .env.example .env  (fill in real API keys)
#
# THIS FILE IS THE DEV TOPOLOGY, and Compose applies it automatically whenever it
# sits beside docker-compose.yml — i.e. in every source clone. It publishes the API
# on :8001 without authentication and PostgreSQL on :5433 with the compose default
# credentials, and runs the backend with hot reload. That is a debugging topology,
# not an install. For a real install: `docker compose -f docker-compose.yml up -d`.
# The backend therefore reports APPLIRE_TOPOLOGY=dev — a startup WARNING and
# GET /health.topology — so "which topology am I running?" has an answer that does
# not depend on remembering which files are on disk (ADR-087 cl. 9, JF-O-1.2).

services:
  postgres:
    ports:
      - "5433:5432"

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8001:8000"
    environment:
      APPLIRE_TOPOLOGY: dev
    volumes:
      - ./backend:/app:z
    command: uvicorn applire.main:app --host 0.0.0.0 --port 8000 --reload

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: dev
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: ""
      BACKEND_URL: "http://backend:8000"
    volumes:
      - ./frontend:/app:z
      - /app/node_modules
      - /app/.next

  retention:
    build:
      context: ./backend
      dockerfile: Dockerfile
    volumes:
      - ./backend:/app:z

  mcp:
    build:
      context: ./backend
      dockerfile: Dockerfile
    volumes:
      - ./backend:/app:z

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/dev.conf:/etc/nginx/conf.d/default.conf:ro,z
    depends_on:
      - backend
      - frontend
